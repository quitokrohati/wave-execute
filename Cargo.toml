[package]
name = "wave_executor"
version = "0.1.0"
edition = "2021"

[dependencies]
gtk = "0.9.0"
tokio = { version = "1", features = ["full"] }
reqwest = { version = "0.11", features = ["json"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lib]
crate-type = ["cdylib"]